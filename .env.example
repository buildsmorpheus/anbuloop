# Add one provider key locally. Never commit .env.local or a real key.
# With both keys present, AnbuLoop offers a small provider picker for new exchanges.

# OPENAI_API_KEY=
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3.1-flash-lite
